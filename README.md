# apache-log-slow-query
This small tool helps to list and identify slow queries from an apache log file with time information.


## Prerequisites

```bash
  sudo gem install --no-ri --no-rdoc apachelogregex
```


### Usage

```bash
``` 
